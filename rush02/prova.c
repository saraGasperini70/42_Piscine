#include <unistd.h>
#include <stdlib.h>

int	main(int ac, char **av)
{
	if (ac == 2)
	{
		//function negative
		if av[1][0]
			// accedi al dizionario
		else if av[1][1]
			if av[1] >= 10 && av[1] <= 12
				//caso particolare 1
			else if av[1] >= 13 && av[1] <= 19
				// caso particolare teen
				// scompongo in cifre, scrivo la cifra finale + "teen"
				if av[1] == 15
					//caso particolare
            //modulo 10 per scomporre in cifre. Se il rest = 0 allora accedi al dizionario
			else
				//tutti gli altri casi + func
		else //piu di due cifre

	}
	return (0);
int ft_strlen(char *str)
int	detect_space(int *str)
{
	int i;

	i = 0;
	while (str[i])
	{
		if ((str[i] >= 9 && str[i] <= 13) && str[i] == 32)
			i++;
	}
	str = str[ft_strlen - i]
	return (*str)
				
}
